extend base

block article 
  fieldset.section
    h1 最近对话的人
    .mod
      ol
      each s in people
        li
          .row
            label ID
            input(type="text", readonly, value=s._id)
          .row
            label 详情
            pre
              | #{JSON.stringify(s, null, '    ')}
