extend base

block article 
  form(method="post")
    fieldset.section
      h1 管理用户
      p.stats 当前共有 #{total} 位用户
      table.table
        tr
          th &nbsp;
          th city 
          th douban
          th silented 
          th active lottery
        each p, i in people
          tr
            td #{i+1}
            td #{p.city}
            td
              a(target="_blank", href="#{p.douban_url()}") #{p.name}
            td #{p.silented ? 'YES' : 'NO'}
            th #{p.active_lottery}
